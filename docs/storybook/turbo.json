{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "dev": {
      "dependsOn": ["dev"]
    },
    "build": {
      "outputs": ["storybook-static/**"]
    },
    "deploy": {
      "dependsOn": ["build"]
    }
  }
}
