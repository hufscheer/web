export type LeagueType = {
  leagueId: number;
  name: string;
  maxRound: number;
  inProgressRound: number;
  isInProgress: boolean;
};

export type SportsType = {
  name: string;
  sportId: number;
};
