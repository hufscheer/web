{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "globalEnv": ["NEXT_PUBLIC_*", "API_BASE_URL", "NODE_ENV", "NEXT_RUNTIME", "CI"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "lib/**", "dist/**"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "lint:fix": {
      "dependsOn": ["^lint:fix"]
    },
    "format": {
      "dependsOn": ["^format"]
    },
    "deploy": {
      "dependsOn": ["^build", "^lint"]
    },
    "dev": {
      "dependsOn": ["^build"],
      "cache": true,
      "persistent": true
    }
  }
}
